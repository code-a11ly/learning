// FUNCTIONS IN JAVASCRIPT

/*
    okay so when you want to execute a code
    but what if you have to execute the same
    code repeated times along the code? that
    is where functions come in
*/

function soma(a, b) {


    return (a + b);
};

var result = soma(1, 2)